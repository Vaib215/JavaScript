// Event and other DOM Properties

// InnerHTML and outerHTML
// InnerHTML - returns/manipulate the internal html of any tag as a string
// OuterHTML - Self as starting tag + innerHTML + self as closing tag => returns/manipulate
// suppose p.innerHTML is "I am a para"
// then p.outerHTML is "<p> I am a para</p>"

// textContent - returns/manipulate only the text of the element
// hidden - returns boolean - return/manipulate the hidden/attribute of an element


// Attribute methods
// hasAttribute(name) - return boolean on the basis of presence of an attribute in an element
// getAttribute(name) - returns the value of an attribute of an element
// setAttribute(name,value) - changes/creates the value for an attribute of an element
// attributes - returns the collection of attributes (updates on any change)

// data-* attributes
// custom attributes made for developers to access some values in javascript
