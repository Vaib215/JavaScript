/*
console.log("Hello JS")


// Variables in javascript
// These are containers which are used to hold some value
// Cheeni ka dabba: cheeni

// In JS, we can use variables to store numbers, strings, objects, array, etc

// There are 3 methods to declare variables in js
// 1. Using keyword const
// Variables that are declared only once.
// Ex: API_KEY, ADMIN_USERNAME
const API_KEY = "45dfvdfv4ddfg5df5g5df"
console.log(API_KEY)
// API_KEY = "45454" -> gives error

// 2. Using keyword let
// Variables that can be updated again and again
// Ex: Count, isLoggedIn
let count = 1;
console.log(count)
count = 5
console.log(count)

// 3. Using var keyword
// Variables that have global scope and can be updated again and again.
// Note: Avoid using var keyword
var username = "Vaib"
console.log(username)
username = "Vaibhav"
console.log(username)
*/
// Differences b/w let, const and var
/*
variables that can are declared using let keyword can be updated again and need not to be initialized at the time of declaration

variables that are declared using const need to be initialized at the time of declaration

avoid declaring variables using var keyword because it creates variables that have global scope which is a bad practice

let fruit
fruit = "mango"
console.log(fruit)

const game
game="football"
console.log(game)
*/
// let, const and var


// var friend = "Sonu"
// console.log(friend)

// var friend = 5
// console.log(friend)